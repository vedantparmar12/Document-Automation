{
  "mcp.servers": [
    {
      "name": "document-automation",
      "command": "python3",
      "args": [
        "${workspaceFolder}/src/server.py"
      ],
      "env": {
        "PYTHONPATH": "${workspaceFolder}"
      }
    }
  ]
}